name: Prod Push

on:
  push:
    branches:
      - main

jobs:
  main-build:
    uses: cosmicmind/github-actions/.github/workflows/main-build.yml@main
  test:
    needs: main-build
    uses: cosmicmind/github-actions/.github/workflows/test.yml@main
  package-publish:
    needs: test
    uses: cosmicmind/github-actions/.github/workflows/package-publish.yml@main
